import React, { Component } from 'react';

class DVRCameras extends Component {
    render = () => (
        <fieldset>
        <legend>Cameras</legend>
        </fieldset>
    );
}

export default DVRCameras;